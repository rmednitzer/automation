# Development dependencies for the automation Ansible repository.
#
# Pinned versions are bumped by Renovate's pip manager; see
# renovate.json5. To install locally:
#
#   make install
#
# which runs `pip install -r requirements-dev.txt` plus
# `ansible-galaxy install -r requirements.yml`.
#
# CI mirrors this file from .github/workflows/ci.yml (lint, syntax-check,
# and validate-compliance jobs all install from here so the toolchain
# stays consistent across local and CI runs).

ansible-core==2.21.0
ansible-lint==26.4.0
yamllint==1.38.0
pre-commit==4.6.0
PyYAML==6.0.3
# Optional for scripts/validate-compliance-controls.py: when present the
# validator also checks docs/compliance-controls.yml against the published
# JSON Schema (docs/schemas/compliance-controls.schema.json, LIMITATIONS
# L6). The validator degrades to its structural checks if this is absent, so
# it is a dev/CI convenience, not a runtime dependency.
jsonschema==4.26.0
