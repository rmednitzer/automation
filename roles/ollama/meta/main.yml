---
galaxy_info:
  role_name: ollama
  author: automation
  description: >-
    Local LLM inference runtime (Ollama) — pinned, checksum-verified install
    with a hardened, localhost-bound service, for dedicated inference hosts on
    Ubuntu 24.04 / 26.04 LTS. Keeps inference on-premises (data sovereignty).
  license: Apache-2.0
  min_ansible_version: "2.18"
  platforms:
    - name: Ubuntu
      versions:
        - noble      # 24.04 LTS
        - resolute   # 26.04 LTS (kernel 7.0) — see docs/ADR-004
  galaxy_tags:
    - ollama
    - inference
    - llm
    - ai
    - gpu

dependencies: []
